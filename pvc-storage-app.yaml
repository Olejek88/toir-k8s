apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-disk
  labels:
    app: app
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
